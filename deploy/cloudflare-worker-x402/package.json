{
  "name": "attestseal-x402-stamper",
  "version": "0.1.0",
  "description": "Cloudflare Worker that stamps X-AttestSeal-* trust attestation headers on 402 responses.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.40.0"
  },
  "license": "MIT"
}
